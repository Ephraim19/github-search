import { SearchProfileDirective } from './search-profile.directive';

describe('SearchProfileDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchProfileDirective();
    expect(directive).toBeTruthy();
  });
});
